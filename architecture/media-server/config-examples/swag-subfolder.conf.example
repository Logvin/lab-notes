# SWAG subfolder proxy configuration examples
#
# Place these in /config/nginx/proxy-confs/ within the SWAG container.
# Each file handles routing for one application at a subfolder path.
#
# All services use the /apps/ prefix convention:
#   media.example.com/apps/sonarr
#   media.example.com/apps/radarr
#   media.example.com/apps/prowlarr
#   media.example.com/apps/tautulli
#   media.example.com/apps/overseerr
#   media.example.com/apps/portainer
#
# IMPORTANT: Each application must have its URL Base set to match.
#   Sonarr:   Settings > General > URL Base = /apps/sonarr
#   Radarr:   Settings > General > URL Base = /apps/radarr
#   Prowlarr: Settings > General > URL Base = /apps/prowlarr
#   Tautulli: Settings > Web Interface > HTTP Root = /apps/tautulli

# ============================================================
# sonarr.subfolder.conf
# ============================================================
location /apps/sonarr {
    return 301 $scheme://$host/apps/sonarr/;
}

location ^~ /apps/sonarr/ {
    # Enable Cloudflare Access JWT validation (if using lua module)
    # include /config/nginx/cf-access-auth.conf;

    set $upstream_app 127.0.0.1;
    set $upstream_port 8989;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    # Standard proxy headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    # WebSocket support (needed for real-time updates)
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;

    # Increase timeouts for large API responses
    proxy_read_timeout 300;
    proxy_send_timeout 300;
}

# Sonarr API - separate location for API key authentication
location ^~ /apps/sonarr/api {
    set $upstream_app 127.0.0.1;
    set $upstream_port 8989;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}


# ============================================================
# radarr.subfolder.conf
# ============================================================
location /apps/radarr {
    return 301 $scheme://$host/apps/radarr/;
}

location ^~ /apps/radarr/ {
    set $upstream_app 127.0.0.1;
    set $upstream_port 7878;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;

    proxy_read_timeout 300;
    proxy_send_timeout 300;
}

location ^~ /apps/radarr/api {
    set $upstream_app 127.0.0.1;
    set $upstream_port 7878;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}


# ============================================================
# prowlarr.subfolder.conf
# ============================================================
location /apps/prowlarr {
    return 301 $scheme://$host/apps/prowlarr/;
}

location ^~ /apps/prowlarr/ {
    set $upstream_app 127.0.0.1;
    set $upstream_port 9696;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;
}


# ============================================================
# tautulli.subfolder.conf
# ============================================================
location /apps/tautulli {
    return 301 $scheme://$host/apps/tautulli/;
}

location ^~ /apps/tautulli/ {
    set $upstream_app 127.0.0.1;
    set $upstream_port 8181;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;
}


# ============================================================
# overseerr.subfolder.conf
# ============================================================
location /apps/overseerr {
    return 301 $scheme://$host/apps/overseerr/;
}

location ^~ /apps/overseerr/ {
    set $upstream_app 127.0.0.1;
    set $upstream_port 5055;
    set $upstream_proto http;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;
}


# ============================================================
# portainer.subfolder.conf
# ============================================================
location /apps/portainer {
    return 301 $scheme://$host/apps/portainer/;
}

location ^~ /apps/portainer/ {
    set $upstream_app 127.0.0.1;
    set $upstream_port 9443;
    set $upstream_proto https;

    proxy_pass $upstream_proto://$upstream_app:$upstream_port;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Ssl on;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $http_connection;

    # Portainer uses HTTPS internally
    proxy_ssl_verify off;
}


# ============================================================
# Security headers (add to server block)
# ============================================================
# add_header X-Content-Type-Options "nosniff" always;
# add_header X-Frame-Options "SAMEORIGIN" always;
# add_header X-XSS-Protection "1; mode=block" always;
# add_header Referrer-Policy "strict-origin-when-cross-origin" always;
# add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
